extend type Query {
  libraries(department: Department!): LibraryCollection!
}

type NodeLibrary {
  id: String!
  name: String!
  version: String!
  dependents: [NodeLibraryDependent!]!
  date: String!
}
type AndroidLibrary {
  id: String!
  name: String!
  version: String!
  dependents: [NodeLibraryDependent!]!
  date: String!
}
type IOSLibrary {
  id: String!
  name: String!
  version: String!
  dependents: [NodeLibraryDependent!]!
  date: String!
}

union Library = NodeLibrary | AndroidLibrary | IOSLibrary 

type LibraryCollection {
  id: Department!
  nodes: [Library!]!
}

type NodeLibraryDependent {
  id: String!
  name: String!
  version: String!
}

enum Department {
  FRONTEND
  BACKEND
  ANDROID
  IOS
}
